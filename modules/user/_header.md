# terraform-azurerm-avm-res-documentdb-mongocluster//modules/user

Submodule for managing MongoDB vCore cluster users (`Microsoft.DocumentDB/mongoClusters/users@2025-09-01`).

Supports native MongoDB users and Microsoft Entra ID users via the `identity_provider` discriminated type.
